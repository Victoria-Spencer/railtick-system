package org.rail.ticketservice.enums;

/**
 * 列车标签（约束合法值）
 */
public enum TrainTag {

    // (0, "复兴号")
    FUXING,
    // (1, "智能动车组")
    INTELLIGENT,
    // (2, "静音车厢")
    QUIET_CAR,
    // (3, "支持选铺")
    SEAT_SELECT
}