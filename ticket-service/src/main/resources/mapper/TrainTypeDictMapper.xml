<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.rail.ticketservice.mapper.TrainTypeDictMapper">

    <select id="getTrainTypeDictByTrainId" resultType="org.rail.ticketservice.pojo.vo.TrainTypeVO"
            parameterType="java.lang.Long">
        SELECT
            ttd.id AS typeId,
            ttd.type_name AS typeName,
            ttd.type_code AS typeCode
        FROM train_type_dict ttd
        INNER JOIN train_train_type ttt
            ON ttt.type_id = ttd.id
        WHERE
            ttt.train_id = #{trainId}
    </select>
</mapper>